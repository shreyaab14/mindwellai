import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mindwellai.app',
  appName: 'MindWellAI',
  webDir: 'dist/public',
  bundledWebRuntime: false
};

export default config;